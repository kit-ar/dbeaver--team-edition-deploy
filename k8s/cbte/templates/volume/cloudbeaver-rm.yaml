apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  creationTimestamp: null
  labels:
    app: {{ .Release.Name }}-rm-data
  name: {{ .Release.Name }}-rm-data
spec:
  accessModes:
    - ReadWriteMany
  {{- if or (eq .Values.cloudProvider "gcp") (eq .Values.cloudProvider "aws") (eq .Values.cloudProvider "azure") }}
  storageClassName: {{ .Values.storage.storageClassName }}
  {{- else }}
    {{- if .Values.cloudbeaverRM.storageClassName }}
  # adding support for custom storage class (independent of whether we declare we are in a specific cloud or not)
  storageClassName: {{ .Values.cloudbeaverRM.storageClassName }}
    {{- else }}
      {{- if .Values.storageClassName }}
  storageClassName: {{ .Values.storage.storageClassName }}
      {{- end }}
    {{- end }}
  {{- end }}
  resources:
    requests:
      storage: 2Gi
status: {}
